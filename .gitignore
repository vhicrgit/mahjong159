.venv/
__pycache__/
*.pyc
*.pyo
*.pt
*.npz
.DS_Store
*.egg-info/
dist/
build/
*.bak
.gradle/
local.properties
android/build/
android/app/build/
**/build/
*.apk

# IDE
.qoder/

# 本地临时脚本与调试产物(不入库)
# 约定: 以下划线开头的文件均为一次性探测/验证脚本与截图
# (正式代码均在 js/ css/ backend/ 等子目录下, 不会被误包含)
_*.js
_*.json
_*.html
_*.txt
_*.png
_*.jpg
_*.c
mobile/wasm/_bench

# wasm 构建副产物: 原生对拍用的动态库(平台相关, 不入库)
# 由 mobile/wasm/build.sh 生成; 入库的是 mjcore.wasm 与 js/mjcore_wasm.js
*.dylib
.venv-jax/
.jax_cache/
models/*.bin
!models/suit_front_table.npz

# mjcore 原生编译产物(由 backend/analysis/hv_native.py 自动生成)
backend/analysis/libmjcore.so

# 外部依赖目录(不入库): Mortal 仓库克隆(自带 .git) + mortal_298k.pth(130MB,
# AGPL-3.0, 超过 GitHub 单文件上限) + tools/mortal_selfplay.py 生成的 mjai 牌谱。
# 复现 Mortal 蒸馏实验时本地准备:
#   git clone https://github.com/Equim-chan/Mortal third_party/Mortal
#   权重与 config.toml 放到 third_party/mortal-298k/
#   libriichi 的 .so 构建到 /dev/shm/mortal_target/release(见 tools/mortal_selfplay.py)
third_party/
