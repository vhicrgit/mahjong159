# 规则 Bot 与分析器评审落地记录（2026-09-05 两轮外部评审）

前置阅读：[独立审计](independent_audit_20260905.md) 与
[修复和推进记录](recovery_progress_20260905.md)（同日上午我们自己做的一轮）。
本文记录**外部评审（Codex/GPT）的意见裁决**、本轮代码改动、以及两个新测出来的
重要数字：完整协议下的重基线、回报的条件方差分解。

材料：`~/old/codex_idea/` 下的 `安康159麻将AI训练诊断.md`、
`规则Bot与牌型价值分析器改进研究.md`、`规则Bot研究交接包.zip`、`benchmark_159.py`。
两轮评审看的都是 `fe68da8`（2026-08-30）快照，落后当时 HEAD 16 个提交。

**口径提醒**：评审引用的 `grpo_train.py / mj159.c / bot_v1.py / ppo.py /
env159.py / paired_eval.py` 自 `fe68da8` 起一行未改，所以它的静态结论对当前代码
仍然成立；但它没看到我们当天上午的审计修复（合法动作掩码、四家统一暗杠/补杠、
seed 聚类 SE、多隐藏世界采样、逐世界配对回报），那几条 P0 有一半已经修掉了。
本文所有新数字都在**我们自己的台架**上跑（`backend/rl/eval_crn.py`：四座位轮转 +
seed 聚类 SE + 完整动作协议 + 真实净分），种子段与对方完全不同。

---

## 一、第一轮评审（训练诊断）逐条裁决

| 评审断言 | 我们的复现 | 裁决 | 处置 |
|---|---|---|---|
| C 原生进张原语在四副露后仍错处理单钓 | 随机 400 手四副露：**进张集不一致 97.8%**，选牌不一致 21.5%（`logs/audit_codex_20260905.json`） | ✅ 真 | **已修** `mj159.c` 新增 `shanten_raw`，让 `tiles_info/ukeire` 也走 need==0 特判；修复后同批 400 手不一致 0%（残留 1% 是浮点平局） |
| 但影响面有多大 | 300 局 4×NativeV31 的 6839 个弃牌决策里，**四副露出现 0 次**（副露数分布 0/1/2/3 = 5040/1566/217/16） | ⚠️ 真实但当前对局几乎不触发 | 仍然修：`bot_native.py` 声称"与 v10/v31 逐位同口径"，不能留反例 |
| features_v2 是否被牵连 | `native.shanten / discard_shanten / waits_ukeire` 对 200 个短手牌**零不一致**（它们走 `mj_shanten`/`mj_is_win`，特判已覆盖） | ❌ 不受影响 | **现役 NN 无需因此重训** |
| `bot_v1.choose_discard` 的 visible 计数依赖绝对座位 | 同信息下座位0 与座位3 **选牌分歧 21.5%**（`if q.seat == self.seat` 写在 for 循环外，只有座位3 把自己手牌计入 visible） | ✅ 真 | **不修**：`bot_native.NativeV1` 的注释已声明刻意复刻此行为；改它会静默改变历史基线（评审自己也建议"保留旧 v1 作历史版本，另建修正版"）。现役基线是 v31n，不受影响 |
| GRPO 的带符号 log-prob 损失无下界 | 本机探针（两动作 A=[+1,−1]、均匀参考、β=0.1）：logit 差 10 → loss −9.9307；差 100 → −99.9307；差 1000 → −999.9307，KL 恒饱和在 log2，dloss/dlogit 恒 −1 | ✅ 真（数学正确） | **不改代码，加退役警告**：`grpo_train.py` 已退役（只被 `tools/perf/` 的诊断脚本 import），现役 `dagger_train.policy_loss` 正是评审建议的"归一化非负目标 + 真实合法掩码交叉熵" |
| GRPO 快照只覆盖整局第 2~13 次弃牌 | `rng.sample(range(2,14))` 配全局弃牌计数器，确认只采到每家前 1~4 巡 | ✅ 真 | 同上；现役采集器（`cf_collect`/`dagger_collect`）是按概率全轨迹采样，无此偏置 |
| PPO 采样温度与更新 logits 不一致、value 单位不一致 | 指 `backend/rl/ppo.py`（退役）。现役 `tools/rl_bloody_train.py` 的 `LP0` 与更新端同为掩码后 logits，采集温度一致；advantage 用 R−v 后再整体标准化，无 GAE 单位混用 | ⚠️ 对退役代码成立 | 不改；现役链路无此问题 |
| JAX `env159` 补杠合法动作被空副露槽覆盖 | 纯 jax 复现：`.at[[0,5,0,0]].set([True,True,False,False])` → index0=False；`.max()` → True。真实转换状态验证：碰一条后 action 55 修复前非法、修复后合法（二条/万9 对照正常） | ✅ 真 | **已修**（`uint8` 上做 `max` 归约，顺序无关）。JAX 线目前只被 bench 脚本用，修掉是为了不留陷阱 |
| `1-(1-W/T)^k` 应为超几何 `1-C(T-W,k)/C(T,k)` | 数学正确 | ✅ 真但只在 `bot_v28/v29`（已证伪归档实验） | 不修（改归档实验会让历史记录与代码不符）；数值对照存在 `logs/audit_codex_20260905.json` |
| `Game.public_state()` 泄露对手摸牌等私有信息 | `last_drawn={'seat':2,'tile':13}` 直发前端；`gang_options` 给的是当前行动家（可能是别家）；`log` 里有"座位2 摸牌 五万" | ✅ 真 | **已修**：按 `for_seat` 脱敏（别家摸牌只留 seat、杠选项只在轮到自己时给、`pending_actions` 只回自己那条、日志去掉别家摸牌牌面）。前端只用 `pending_actions["0"]`/`last_drawn.seat===0`/`turn===0` 的杠选项，UI 不受影响 |
| 评估未执行暗杠/补杠、SE 把四座位当独立样本、N 次 rollout ≠ N 个隐藏世界、硬标签零策略梯度 | —— | ✅ 真 | **当天上午已独立修完**，见 [recovery_progress_20260905.md](recovery_progress_20260905.md) |
| "35~38% 是盲打上限""出牌侧已饱和""PPO 不适合 159"超出证据 | 同意 | ✅ | README 已加更正批注；本文第五节用正确口径重测了方差 |

复现脚本：`tools/perf/audit_codex_claims.py`（只读诊断，输出
`logs/audit_codex_20260905.json`）。

---

## 二、第二轮评审（规则 Bot 与分析器）逐条裁决

### 2.1 听牌后的选择性碰牌 —— 复验通过，已接入为 v32

评审指出 v31 的 `decide_peng` 只接受"碰后向听严格下降"，已听牌时碰完再弃一张
通常仍是 0 向听，于是**即使听口大幅变宽也会被拒**。他们的候选 `ReadyPengBot`
在三个保守条件下才多碰：① 已听牌且 v31 原本拒碰；② 存在碰后弃牌使新的**活听口**
严格包含旧活听口（原听口一张不丢）；③ 名义剩余摸牌次数不减少（碰完必须弃牌，
不会白摸一张，所以"碰一定抢到自己下一摸"是错的）。

我们的独立复验（`backend/ai/bot_v32.py`，全新种子段，对手 3×v31n，四座位轮转，
完整动作协议，主指标真实净分）：

| 批次 | 种子段 | 种子数 | 真实净分差/局 ± SE | t | 名次奖励差/局 ± SE |
|---|---|---:|---:|---:|---:|
| 评审开发批（他们的数字） | 159094000 | 512 | +0.04248 | — | — |
| 评审独立批（他们的数字） | 159200000 | 4096 | +0.01685，CI [+0.0023,+0.0314] | — | — |
| **我们开发批** | 206200000 | 512 | −0.0063 ± 0.0146 | −0.44 | +0.0000 ± 0.0055 |
| **我们独立批** | 206300000 | 4096 | **+0.0215 ± 0.0077**，CI [+0.0065,+0.0366] | **+2.80** | +0.0069 ± 0.0023 (t=+3.05) |

（`logs/v32_holdout4096.log`）512 种子那批测不出来是样本量问题：SE≈0.0146，
要分辨 +0.017 需要 4000 种子量级 —— 这正是 README 里"300 seed 的 t>2 必须换
种子复核"那条口径警告的又一次印证。

机制核对：seed 766107 座位2、上家打 6条，v31 拒碰、v32 碰（暗手变成
678条/789饼/567万 + 单红中，红中可与任意牌成将 → 活听 21 张扩到 94 张）。
注意这一局里 v31 最终 +12 分、v32 只有 +3 分（159 翻牌是随机的）—— 单个牌例
不能当收益，收益只来自 16384 局的平均。触发频率也很低：他们那边 16384 局只有
160 局结果被改变。

已接入：`backend/ai/bot_v32.py`、`roster` 新档位 `v32`（显示名"老鸟+"）、
`tools/perf/eval_ckpt.py` 的 `make_factory` 支持 `v32`。**默认阵容 ROSTER 未改**
（换默认对手是产品决定）。

**NN 侧不接（实测空操作）**：部署用的 `NetBot` 碰杠走分析器 E，我们把同一条
判据（抽成模块级 `ready_peng_plan`）也接上去做了配对 A/B：1500 局、1047 次碰
机会里判据命中 17 次，而这 17 次 **E 判据全部已经接受碰**，净增量 0；1500 种子
配对 A/B（deploy 口径 vs 只有 E 判据）两臂 6000 局**逐局结果完全相同**
（+0.0000 ± 0.0000，`logs/netbot_ready_peng_ab.log`、
`logs/probe_ready_peng_netbot.log`）。所以只在规则 Bot 上保留这条分支，NN 侧
撤回，免得多一条永不触发的代码路径。这也印证了评审自己的判断："这个缺口主要
属于 v31"。

### 2.2 分析器 E：四项已修、两项属模型近似

评审列了 6 项，全部在我们的代码上复现（`hv_math/deterministic_checks.py`
指向本仓库跑，修复前后对照如下）：

| 问题 | 修复前 | 修复后 | 处置 |
|---|---|---|---|
| 碰分支没落实"同向听取进张最宽"（注释与代码不符，C 同样） | 碰3条后选打6条：进张7、后继 E=12.375；而打2饼进张10、E=9.0 | 选打 **2饼** | **已修**（Python + C + JS + Android 四处同步） |
| 浮点牌墙先验被截断成整数 | `u_eff` 给 1万=0.8/9万=0.2，C 的 int8 牌池把两者都变 0 → 选打 1万（进张4.2） | 选打 **9万**（进张4.8） | **已修**：`_fast_discard` 检测到小数牌池时走 Python 口径 `_fast_discard_py`（整数牌池仍走 C，性能路径不变） |
| `held_exp` 路径把牌墙扣成负数 | E 递推里出现 `{'3条': -1}` | 负牌池状态 **0 次** | **已修**：碰的第三张来自对手持牌时不再扣牌池（`u_eff` 已扣过持有期望） |
| 换型层(kaizen)并非"只改绝对值不改排序" | 40 个真实近听状态里 7 个改首选、6 个打破非平局排序 | —— | **确认，不改代码**：评审的证据成立，代码注释的绝对表述过强。我们此前"kaizen 只改绝对值"的说法要收回；换型深度是否值得开，应按分歧局面独立验证，不按"E 降得多"挑参数 |
| 多步 E 只扣命中牌、不扣等待期间摸走的无效牌 | 6 张池 720 排列穷举：E=5.3333，精确均值 **4.6667** | 未修 | **确认，暂不修**：这是模型近似（首次命中公式 `(N+1)/(U+1)` 单层正确，逐层相加不等于多步精确期望）。修它要动 E 递推本体并同步 Python/C/JS/Android，会让所有已训练的 E 标签失效，需要单独立项 |
| 碰通道速率被当成实体有效张套无放回公式 | rho=1 构造例 N=6、U=24 | 未修 | **确认，暂不修**：与上一条同属 E 模型语义，"精确无放回"的表述不成立，面板文案应改为"当前近似模型下的牌型速度" |

C↔Python 对拍（`tools/perf/test_hv_c_parity.py`）在修复后 **累计不一致 0**
（kai_max=1 共 11 手、kai_max=2 共 5 手，`logs/test_hv_c_parity_after_peng_fix.log`）。
E 值确实变了（同一手 5饼 从 15.15 → 15.06），所以**旧的 E 标签与现役 NN 的
碰杠判据都建立在修复前的分析器上**，这点在下一轮重训时必须重采数据。

另外评审提醒的一条产品级口径问题成立：桌面推荐/复盘走 `Analyzer` 的向听+进张
排序，手机分析面板开启后走 E 推荐，学者默认关 kaizen —— 同名"AI推荐"不一定
来自同一策略，比较改进前后必须记录实际推荐源。

### 2.3 评审建议但我们不采纳/暂缓的

- **`DeadWaitGangBot`（听口变死就拒杠）**：评审自己没有增益证据，且他们的
  配对推演显示杠分为正时牺牲听口是划算的（补6饼把活听 24 压到 11，但杠的
  场均分 +8.313 > 不杠 +7.357）→ 不接入。
- **有限窗概率 P₂（K≤3 Bellman）**：模型内核自洽（648 项参考核验误差 0，
  K=2 中位 0.87ms），但只在最后 1~2 次名义摸牌启用时 4096 局赢家与结算分
  全部相同 → 先当面板/解释功能，不作竞技增强。
- **`benchmark_159.py`**：它做的四座位配对、四家统一碰杠、零和检查、截断报错、
  种子聚类区间、版本 hash 记录，我们在当天上午的审计里已在 `eval_crn` /
  `eval_recovery` 中实现，不再引入第二套台架。

---

## 三、本轮代码改动

| 文件 | 改动 | 验证 |
|---|---|---|
| `backend/native/mj159.c` | 新增 `shanten_raw`，`tiles_info` 两处改用它（四副露 need==0 特判覆盖内部进张链） | `test_parity_prims`（新增进张链对拍）+ `test_parity_native` PARITY OK + 400 手四副露不一致 0% |
| `backend/native/test_parity_prims.py` | 新增 `check_ukeire_chain`：`score_discards_v10` vs `bot_v31` 副露感知参考，覆盖 total=2/5/8/11/14 | 666 手 0 不一致，cont 最大差 7.1e-15 |
| `backend/native/native.py`、`backend/analysis/hv_native.py` | staleness 判断纳入共享的 `hv_engine_inc.c`（此前改共享引擎不会触发重建，对拍会得出假的"一致"） | 删 .so 后自动重建 |
| `backend/ai/test_parity_native.py` | 区分**浮点平局**与真实分歧（C 与 Python 求和顺序不同，同分候选 argmax 各倒一边） | v10/v31 真实分歧 0/669，平局 2 次，整局 0/10 不一致，PARITY OK |
| `mobile/wasm/hv_engine_inc.c` | 碰通道改两遍：先最小向听，再只在该组比 `hve_ukeire` | 与 Python 对拍 0 不一致 |
| `mobile/js/hand_value.js` + `android/.../js/hand_value.js` | 同上（两份副本字节一致） | 静态同步（JS 无自动对拍） |
| `backend/analysis/hand_value.py` | 碰分支同向听取进张最宽；`_fast_discard_py` 处理小数牌池；`held_exp` 路径不再把牌池扣成负数 | 三个确定探针全部翻转/清零 |
| `backend/game/engine.py` | `public_state` 按视角脱敏 + `_viewer_log` | `test_audit_fixes` 新增用例 |
| `backend/jax159/env159.py` | 补杠合法动作改用 `uint8` 上的 `max` 归约 | 碰一条/二条/万9 三例均合法 |
| `backend/rl/grpo_train.py` | 退役警告（无下界损失 + 前盘偏置采样 + 现役替代路径） | 仅文档 |
| `backend/ai/bot_v32.py`（新） | v31 + 听牌后选择性碰牌；判据抽成模块级 `ready_peng_plan` 便于单独测量 | 4096 种子独立复验 +0.0215±0.0077 |
| `backend/rl/net_bot.py` | `build_model` 带上 `feat_dim`（718 维 v4 权重此前会崩）；选择性碰牌接上后又撤回 | 1500 局探针：判据命中 17 次、E 全部已接受 → 净增量 0，A/B 6000 局逐局相同 |
| `backend/ai/roster.py`、`tools/perf/eval_ckpt.py`、`tools/perf/eval_h2h.py` | v32 档位（默认阵容未改）；`--raw-score`（真实净分主指标）、`--first-win-only`、`--claim deploy`（直接跑线上 NetBot）、`--claim-a/-b`（两臂不同碰杠口径）、`build_model` 带 `feat_dim` | —— |
| `tools/perf/audit_codex_claims.py`（新） | 评审断言的只读复现脚本 | `logs/audit_codex_20260905.json` |
| `tools/perf/diag_conditional_variance.py`（新） | 条件方差分解（冻结可见历史 × 多隐藏世界 × 多次续盘） | 见第五节 |

---

## 四、完整协议下的重基线（重要修正）

同日上午把"四家统一执行暗杠/补杠"补进评估协议后，**旧数字全部作废**。
3000 种子、四座位轮转、对手 3×v31n、CRN 配对、主指标真实净分：

| 策略 | 首胡真实净分差/局 ± SE | t | 血战真实净分差/局 ± SE | t |
|---|---:|---:|---:|---:|
| **v32**（v31 + 选择性碰） | **+0.0215 ± 0.0077**（4096 种子） | **+2.80** | —— | —— |
| v31n（基线，定义上为 0） | 0 | —— | 0 | —— |
| 分析器本体 hv0（学者口径，修复后） | **−0.1575 ± 0.0329** | **−4.79** | —— | —— |
| 冠军 NN `acnn_latest_best`(=`bc_k0_r2`) + HV 碰杠 | **−0.2037 ± 0.0413** | −4.94 | −0.2469 ± 0.0526 | −4.70 |
| `bc_r2_s3`（q×3 校准版） | **−0.2013 ± 0.0412** | −4.89 | −0.2438 ± 0.0526 | −4.63 |

另有两组头对头（1500 种子，首胡，真实净分）：v32 − 分析器 = **+0.0980 ± 0.0591**
（t=+1.66，CI [−0.018,+0.214]），名次奖励 +0.0327 ± 0.0178（t=+1.83）；
NN − 分析器 的直接配对见 `logs/nn_vs_scholar_new_protocol.log`。
（其余出处：`logs/rebaseline_new_protocol_20260905.log`、
`logs/scholar_vs_v31n_new_protocol.log`、`logs/v32_vs_scholar.log`、
`logs/v32_holdout4096.log`）

**结论（当前排序 v32 > v31n > 分析器 ≳ NN，后两者差距未分辨）**：

1. README 里"NN ≈ 分析器 ≈ v31n 三线打平"是**漏掉自家杠的协议**下测得的，作废。
2. **教师本身就显著弱于规则 Bot**：分析器 hv0（学者口径、碰分支修复后）在
   4096 种子下 **−0.1575 ± 0.0329（t=−4.79，CI [−0.222,−0.093]）**，名次奖励
   −0.0469 ± 0.0102（t=−4.58）。1500 种子那次给的是 −0.076 ± 0.059（不显著）
   —— 样本量不足，不是矛盾。NN 则是 −0.204 ± 0.041（t=−4.94，两个检查点一致）。
   师生直接配对（1200 种子，同 seed 同座位）NN − 分析器 = **−0.061 ± 0.074**
   （t=−0.82，不显著）。合起来读：**NN 那 −0.20 里绝大部分是跟着教师一起掉下去
   的，模仿本身的损耗分辨不出来**。~~学生比教师低 0.13 分/局~~ 已撤回（那是两次
   独立测量相减，间接口径 −0.128 ± 0.072，t≈−1.8，同样不显著）。
   **必须记下的 caveat**：这个 4096 种子数字是碰分支修复**之后**测的，我们没有
   修复前的同规模对照，所以不知道这次修复让分析器变强还是变弱（修复方向与
   注释/v10 口径一致，逻辑上应更准）。要定论得跑一次修复前/后的 4096 种子 A/B。
3. **协变量漂移假设已实测否掉**：用修复后的采集器（四家统一暗杠/补杠）+
   修复后的分析器重采 **73988 条**首胡数据（NN 自身分布 rollout，kai=0 E 标签），
   从 `bc_r2_s3` 热启动重训 4 个模型（只用新数据 / 新+旧锚定混合，各 2 个训练
   种子），3000 种子配对评估**全部与冻结起点不可区分**：

   | 模型 | 相对 bc_r2_s3 的真实净分差/局 | t | 名次奖励差 | t |
   |---|---:|---:|---:|---:|
   | fw_only_s71 | +0.0103 ± 0.0405 | +0.25 | −0.0014 ± 0.0127 | −0.11 |
   | fw_only_s72 | −0.0086 ± 0.0408 | −0.21 | −0.0072 ± 0.0127 | −0.56 |
   | fw_mix_s71 | +0.0103 ± 0.0380 | +0.27 | −0.0102 ± 0.0117 | −0.87 |
   | fw_mix_s72 | −0.0224 ± 0.0373 | −0.60 | −0.0161 ± 0.0114 | −1.41 |

   （`logs/retrain_fix_20260905/`：manifest.json 有完整命令，eval3000.json 有
   逐种子原始矩阵，60000 局 81s）
   所以 −0.20 的缺口**不是**"训练数据来自旧协议"造成的。剩下的最可能解释是
   **教师目标本身**：分析器最小化"期望胡牌巡数 E"，而 E 最小 ≠ "在他人之前、
   黄庄之前胡牌的概率"最大 —— 这正是第一轮评审 §5 的批评。要超过 v32，需要
   以**真实结算分**为目标的教师（搜索/RL），而不是把 E 模仿得更准。
4. **v32 是目前测得最强的非作弊策略**，验收对手池应当同时包含 v31n 与 v32。
   另外要如实记下一条产品级事实：native 规则 Bot 的速度（v10/v31 整局
   16.9~39.9 局/s/核）与 NN 相当（NN 约 14 局/s/核），而 v32 比 NN 强
   0.22 分/局 —— 也就是说 **NN 目前既不更强也不更快**，它的价值只在
   "比 Python 分析器快 ~25 倍"和"作为后续 RL 的热启动"。默认阵容要不要
   换成 v32 是产品决定，本轮未改。

---

## 五、条件方差分解：特权 critic 这条路线被否掉

评审批评我们"87~90% 是不可约牌运、完美 critic 的 R² 天花板≈0.10"这个说法
方法论不对（那是用少量手工特征做回归得到的解释力，不是条件方差）——**批评成立**。
用正确口径重测：冻结可见局面 → 采 W 个牌数相容的隐藏世界 → 每个世界用同一策略
跑 K 局，做三层嵌套分解（`tools/perf/diag_conditional_variance.py`）：

| 成分 | temp=1（训练/RL 口径） | temp=0（搜索教师口径） |
|---|---:|---:|
| 可见局面之间（盲打 critic 可学） | 0.6743（**15.7%**） | 0.7696（20.4%） |
| 隐藏世界之间（特权 critic 额外可学） | 1.2407（**28.8%**） | 3.0003（**79.6%**） |
| 策略采样之内（任何状态型 critic 都消不掉） | 2.3890（**55.5%**） | 0.0000（0%） |
| 合计 | 4.3040 | 3.7699 |
| 盲打 critic 拟合 R²（岭回归，按整局留出） | 0.358（目标自身方差口径）→ 折算总方差 **5.6%** | 0.484 → 折算 **9.9%** |
| 特权 critic 天花板（总方差份额） | **44.5%** | 100% |
| 达到天花板后 advantage 噪声可降到 | **1/1.34** | 无采样噪声，剩余不确定性全在世界选择 |

（256 状态 × 32 世界 × 4/1 次续盘，`logs/diag_conditional_variance_temp{1,0}.json`）

三条结论：

1. **旧数字要改口**：盲打 critic 的天花板不是 0.10 而是 0.157（temp=1），
   而实际拟合只拿到 5.6% —— 差距说明特征表示还有空间，但天花板本身不高。
2. **特权 critic（actor 盲打、critic 看对手手牌与牌墙）不值得投入**：理论上它
   是无偏的方差缩减（baseline 不依赖动作即可），但 temp=1 下 55.5% 的方差来自
   策略**决策之后**的采样，任何状态型 critic 都看不见，最好也只能把 advantage
   噪声降到 1/1.34 —— 而我们要分辨的信号是 0.15~0.2 分/局，需要 3 倍以上。
3. **temp=0 下 79.6% 的方差来自"选了哪个隐藏世界"**，这正好解释了搜索教师的
   信噪比墙：单状态 32 个世界的均值 SE ≈ √(3.0/32) ≈ 0.31，与我们此前实测的
   "N=64 时 SE(差)≈0.32、真实分差中位 0.156"完全吻合。要么世界数再上一个量级
   （~650/候选），要么用同世界**配对**差（我们现在的 cross-fit 就是这么做的）。

顺带得到一个便宜的改进方向：既然 temp=1 的主噪声是策略自身采样，配对基线 PPO
（`tools/rl_paired_train.py`）目前只在**牌墙**层面用共同随机数，动作采样是各自
独立的；把均匀随机数也共用（同一串 u 经各自策略的逆 CDF 映射），两臂的动作序列
会高度相关，这才是把 55.5% 那一项压下去的正确做法。

### 5.1 动作级 CRN 实测：收益完全由策略距离决定

已实现为诊断（`tools/perf/diag_action_crn.py`）：每个 seed 跑两局，L 臂
（learner 坐指定座位）与 F 臂（frozen 坐同一座位），**其余三座两局都用 frozen
贪心 + 分析器 E 碰杠**，牌墙由同 seed 共享 —— 于是两局之间唯一的随机源就是该
座位的动作采样。三种口径：`greedy`（F 臂贪心，现行默认）、`indep`（F 臂同温
独立采样，现行 `--frozen-temp`）、`crn`（两臂同温采样且共用同一串 u，按"该座位
第 k 次决策"对齐走逆 CDF）。

自检：两臂同一检查点时 `crn` 的 **sd 恰好 0.0000、动作分歧 0 次**（逐局轨迹
完全一致），证明随机数确实对齐了。

用"对 frozen 加相对幅度 eps 的高斯噪声"制造距离可控的近邻策略，200 seed 扫描：

| eps | KL(L‖F) | argmax 一致率 | sd greedy | sd indep | **sd crn** | crn/greedy | 分歧动作/局 |
|---|---|---|---|---|---|---|---|
| 0 | 0 | 100% | 1.854 | 2.077 | **0.000** | 0（方差 ∞） | 0.00 |
| 0.01 | 0.0032 | 97.3% | 2.004 | 2.109 | **1.014** | **0.51x（方差 3.9x）** | 1.55 |
| 0.03 | 0.0279 | 91.5% | 1.953 | 2.169 | **1.551** | **0.79x（方差 1.6x）** | 3.09 |
| 0.10 | 0.2901 | 78.5% | 2.443 | 2.219 | 2.193 | 0.90x（方差 1.2x） | 5.55 |
| 0.30 | 1.8729 | 53.0% | 2.448 | 2.449 | 2.628 | 1.07x（**更差**） | 7.24 |

（`logs/diag_action_crn_eps*.json`、`logs/diag_action_crn_sweep.log`；
两个真实检查点 bc_k0_r2 vs bc_r2_s3 的对照在 `logs/diag_action_crn.json`：
它们 argmax 完全相同但采样熵差很多，每局分歧 6.3 次，CRN 只有 0.93x —— 印证
"看 KL 而不是看 argmax"）

三点读数：

1. **收益随 KL 单调衰减**：KL≈0.003 有 3.9x 方差收益，KL≈0.028（正好是训练器
   `--kl-target 0.02` 的量级）只剩 1.6x，KL>0.3 反而变差。所以要用它就必须把
   基线保持在近邻 —— 也就是每轮刷新基线（变成标准的 old-policy 基线），而不能
   一直跟固定冠军比。
2. **第二个好处是去掉常数偏移**：greedy 口径的 adv 均值恒在 −0.29~−0.39
   （"采样策略比贪心差"这项，与动作好坏无关），crn 口径下变成 −0.25~+0.06，
   advantage 不再被"学习者要探索"主导。
3. **代价**：F 臂要为每个 hero 座位各跑一局（现行设计一个 seed 只有 1 局全冻结
   基线），即每 seed 从 5 局涨到 8 局（+60%）。在 KL≈0.02 下 8 局 × 1.6x 方差
   ≈ 5 局的噪声，只算小赚；KL≈0.003 才是明显赚。

**结论：机制成立、实现便宜，但它解除不了真正的约束**——每次更新能负担的独立
对局数。因此暂不接进 `rl_paired_train.py`；要接就必须同时改成每轮刷新基线，
并把 KL 压在 0.01 以下，否则收益会被 +60% 的局数吃掉。

---

## 六、已知但未修（含理由）

| 项 | 理由 |
|---|---|
| `bot_v1` visible 计数的座位依赖 | 刻意保留的历史基线行为（`NativeV1` 注释已声明）；改它会静默改变"1个测试Bot+3×v1"这套旧口径的含义 |
| E 递推的多步期望偏差、碰通道 rho 语义 | 属模型近似而非实现错误；修它要同步 Python/C/JS/Android 四份实现并让全部 E 标签失效，需单独立项 + 重新对拍 |
| `bot_v28/v29` 的 `1-(1-W/T)^k` | 已证伪归档实验，不在现役链路 |
| `grpo_train.py` / `ppo.py` / `jax159` 的算法缺陷 | 退役路径；已加警告并指出现役替代。JAX 的补杠掩码是唯一动手修的（3 行，且有确定性复现） |
| kaizen"只改绝对值"的注释表述 | 已被证伪，但改注释前先要决定 kaizen 的默认值；现役学者默认关闭 |
| `backend/native/libmj159.so` 被 git 跟踪 | 它是 `-march=native` 的构建产物，换机器检出可能直接非法指令。本轮改了 `mj159.c` 与共享 E 引擎，所以同步提交了重建后的 `.so`（否则检出到的二进制与源码不一致，而 staleness 判断在新克隆下只靠 mtime、并不可靠）。建议后续移出版本库、只保留本地自动构建 |

---

## 七、下一步（按性价比排序）

1. ~~重采数据 + 重训~~ **已执行，假设被否**（第四节结论 3）：73988 条协议一致的
   新数据 + 修复后的分析器标签 + 4 个重训模型，3000 种子配对全部与冻结起点
   不可区分。数据不是瓶颈。
2. **换教师**（现在已知分析器显著弱于 v31n，这是最要紧的一条）。三条路径按
   性价比排序：
   - **(a) 最便宜：把 BC 的模仿目标从分析器 E 换成 v32 的决策。** 标签用 native
     生成（555 局/s/核），一轮数据 + 训练的代价与本轮重训相当。预期能把 NN 从
     −0.20 拉到 ≈ +0.02（v32 水平）。**但要如实说明**：这只是把 NN 追平规则 Bot，
     而 native v32 本身比 NN 更快 —— 所以它的意义是给后续 RL/搜索蒸馏一个
     "不拖后腿的热启动"，不是产品收益。
   - **(b) 以真实结算分为目标的搜索教师**：多隐藏世界 + 逐世界配对回报 + 交叉
     验证，基础设施已在 `tools/search_teacher.py` / `check_world_teacher.py`；
     缺的是世界数（按 temp=0 下 79.6% 方差来自世界选择推算，需要 ~256-650
     世界/候选，≈100x 当前投入）。这是唯一有希望**超过** v32 的路。
   - **(c) 修完 E 的模型级缺陷再当教师**：优先级最低 —— E 的目标（最小化期望
     胡牌巡数）本身可能就不对齐得分，把它算得更准未必更强。
3. ~~配对 PPO 的动作级 CRN~~ **已实测**（5.1 节）：机制成立（同一检查点下 sd
   恰好为 0），但收益随 KL 单调衰减 —— KL≈0.003 有 3.9x 方差收益，训练器的
   KL 预算 0.02 处只剩 1.6x，而代价是每 seed 从 5 局涨到 8 局。要划算必须同时
   把基线改成每轮刷新的 old-policy 并把 KL 压到 0.01 以下；单独接进现行
   `rl_paired_train.py`（固定冠军基线）不划算，**暂缓**。
4. 分析器 E 的模型级修正（多步期望漏扣无效牌 + 碰通道 rho 语义）单独立项，
   四份实现（Python/C/JS/Android）同步 + 全量对拍 + 重新生成标签。已落实的前提：
   本轮起所有新标签都出自修复后的分析器，旧 `k0_*` 标签不要与新分析器混用。
5. **验收口径固定下来**：对手池同时含 v31n 与 v32；主指标真实净分；四座位轮转 +
   seed 聚类 SE；开发种子段与最终种子段分离（本轮已用 206200000/206300000/
   206900000/207000000/207100000 等互不重叠的段）。
6. **产品状态**：默认阵容座位2 已按用户决定从 v31 换成 **v32**
   （`backend/ai/roster.py`，前端下拉框也加了"老鸟+(v32)"档）。仍待决：
   - NN 档位（AC学者）在既不更强也不更快的情况下是否保留；
   - 移动端/安卓的 JS 侧还没有 v32 实现，`android/.../js/game.js` 的
     `DEFAULT_KINDS` 仍是 `2: "v31"` —— 需要把 `ready_peng_plan` 移植成
     `mobile/js/bot_v32.js` 才能两端一致。
